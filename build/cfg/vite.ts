import type { NuxtConfig } from 'nuxt/schema'

export const viteCfg: NuxtConfig['vite'] = {
  plugins: [
    //
  ],
  server: {
    proxy: {
      //
    },
  },
  css: {
    preprocessorOptions: {
      scss: {
        //
      },
    },
  },
}
